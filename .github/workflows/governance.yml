name: Governança

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify-governance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verificar documentos obrigatórios
        run: sh scripts/verify-governance.sh
