import { model, ui, h, loadRecordDraft } from '../context.js';

export function recordValues(){const existing=ui.state.recordEditingDate?model.state.records.find(r=>r.date===ui.state.recordEditingDate):null,draft=!existing?loadRecordDraft():null;return{date:existing?.date||draft?.recordDate||model.todayKey(),gross:existing?.gross??draft?.recordGross??'',km:existing?.km??draft?.recordKm??'',hours:existing?.hours??draft?.recordHours??'',fuelSpend:existing?.fuelSpend??draft?.recordFuel??'',editing:Boolean(existing)};}
export function recordPreview(values){return model.recordNumbers(model.recordDraft(values));}
export function recordWeekImpact(values){const before=model.weekContext(model.calculations()),numbers=recordPreview(values),after=before.actual+numbers.net,pct=before.target>0?Math.max(0,Math.min(999,after/before.target*100)):0;return{before,after,pct};}

export function renderRecord(){const values=recordValues(),preview=recordPreview(values),impact=recordWeekImpact(values),confirmationDate=ui.state.recordConfirmationDate,confirmationRecord=confirmationDate?model.state.records.find(r=>r.date===confirmationDate):null,confirmationNumbers=confirmationRecord?model.recordNumbers(confirmationRecord):null;return `<section id="view-day" class="view-section ${ui.state.route==='day'?'':'hidden'}" data-block2="ready"><div class="page-head"><button id="r360RecordCancel" type="button" class="back-button" aria-label="Cancelar">←</button><div><span class="eyebrow">Registrar</span><h2 class="title">Como foi seu dia?</h2><p class="subtitle">Faturamento e quilômetros primeiro. O restante é opcional.</p></div></div>${confirmationRecord?`<div id="recordConfirmation" class="confirmation"><span class="eyebrow">Fechamento concluído</span><h3 id="recordConfirmationTitle">${ui.state.lastRecordUpdated?'Dia atualizado':'Dia registrado'}</h3><p id="recordConfirmationText">Este dia agora faz parte da sua semana e do seu mês.</p><div class="metric-grid section-gap"><div class="metric"><small>Faturamento</small><strong id="recordConfirmationGross">${model.money(confirmationRecord.gross)}</strong></div><div class="metric"><small>Quilômetros</small><strong id="recordConfirmationKm">${model.integer(confirmationRecord.km)} km</strong></div><div class="metric"><small>Líquido estimado</small><strong>${model.money(confirmationNumbers.net)}</strong></div><div class="metric"><small>Receita/km</small><strong>${model.money(confirmationNumbers.revenuePerKm)}</strong></div></div><div class="input-row section-gap"><button id="recordDoneButton" class="primary green" type="button">Concluir</button><button id="recordEditButton" class="secondary" type="button">Ver resultados deste dia</button></div></div>`:''}<div class="surface stack section-gap"><div data-record-role="essential-fields" class="stack"><div class="input-group"><label class="input-label">Data</label><input id="recordDate" class="input" type="date" value="${h(values.date)}"></div><div class="input-row"><div class="input-group"><label class="input-label">Faturamento</label><div class="money-input"><span>R$</span><input id="recordGross" class="input" type="number" step=".01" value="${h(values.gross)}"></div></div><div class="input-group"><label class="input-label">Quilômetros</label><input id="recordKm" class="input" type="number" step=".1" value="${h(values.km)}"></div></div></div><details id="recordOptionalDetails" class="details" ${values.editing?'open':''}><summary><span>Adicionar detalhes</span><span>Opcional</span></summary><div class="details-body"><div class="input-row"><div class="input-group"><label class="input-label">Horas online</label><input id="recordHours" class="input" type="number" step=".1" value="${h(values.hours)}"></div><div class="input-group"><label class="input-label">Combustível gasto</label><div class="money-input"><span>R$</span><input id="recordFuel" class="input" type="number" step=".01" value="${h(values.fuelSpend)}"></div></div></div><p class="copy">Você pode salvar sem preencher esta parte.</p></div></details><div class="preview"><span class="eyebrow">Prévia antes de salvar</span><div class="preview-grid section-gap"><div class="preview-metric"><small>Líquido</small><strong id="previewNet">${model.money(preview.net)}</strong></div><div class="preview-metric"><small>Custo total</small><strong id="previewCost">${model.money(preview.fuel+preview.variable+preview.percentCost+preview.fixedShare)}</strong></div><div class="preview-metric"><small>Receita/km</small><strong id="previewRevenueKm">${model.money(preview.revenuePerKm)}</strong></div><div class="preview-metric"><small>Semana após salvar</small><strong>${Math.round(impact.pct)}%</strong></div></div><p id="previewExplanation" class="copy">${values.gross&&values.km?`Este dia levaria sua semana para aproximadamente ${Math.round(impact.pct)}% do planejado.`:'Preencha faturamento e quilômetros para ver a consequência antes de salvar.'}</p></div><button id="saveDayButton" class="primary full" type="button">${values.editing?'Atualizar este dia':'Registrar este dia'}</button><button id="clearDayButton" class="hidden" type="button"></button></div></section>`;}
